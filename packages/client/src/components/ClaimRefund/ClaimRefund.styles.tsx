import styled from 'styled-components';
import { LenderButton } from '../InvestModal/InvestModal.styles';

export const RefundButton = styled(LenderButton)`
&&&& {
    border: 1px solid #CFD0D4;
    background: #FFF;
    color: #5A5A5A;
}
`