import styled from 'styled-components';
import { SubBar as RawSubBar } from '../styles';

export const SubBar = styled(RawSubBar)`
  background: #eb3f93;
  position: relative;
  z-index: -1;
`;

export default SubBar;
